import React from "react";

interface ComparisonSetupProps {
  // Add props interface here
}

const ComparisonSetup: React.FC<ComparisonSetupProps> = (props) => {
  return (
    <div className="comparison-setup">
      {/* Comparison setup form content will go here */}
    </div>
  );
};

export default ComparisonSetup;
